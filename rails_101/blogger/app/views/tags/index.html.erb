<h1>Tags List</h1>

<% @tags.each do |tag| %>
	<li><%= link_to tag.name, tag_path(tag) %> | <%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "Really delete #{tag.name} tag?"}%></li>
<% end %>

